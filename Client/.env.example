# Deploy só na Vercel (API + site no mesmo domínio): deixe vazio.
# O browser chama /api/... no mesmo host.
#
# Só precisa de URL absoluta se a API estiver noutro domínio (sem barra no final).
# Em desenvolvimento local deixe vazio para usar o proxy do Vite (/api -> localhost:3001).
VITE_API_URL=
